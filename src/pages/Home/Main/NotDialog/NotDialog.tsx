import React from 'react'
import s from "./NotDialog.module.scss"

const NotDialog = () => {
	return (
		<div className={s.wrapper}>
			Select the dialog from the left menu
		</div>
	)
}

export default NotDialog